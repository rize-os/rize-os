access-manager:
  clients:
    application-gateway:
      redirect-uri-pattern: 'http://localhost:8080/%{alias}%,http://127.0.0.1:8080/%{alias}%'
---
keycloak:
  realm: rize-os
---
logging:
  level:
    rize.os: TRACE